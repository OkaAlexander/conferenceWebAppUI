import { Grid, makeStyles, Paper } from "@material-ui/core";
import React from "react";

const styles = makeStyles((theme) => ({}), { index: 1 });
export default function ConferenceInfoCard() {
  const classes = styles();
  return (
    <Grid item>
      <Paper></Paper>
    </Grid>
  );
}
